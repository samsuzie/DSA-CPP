#include <bits/stdc++.h> 
int findMedian(vector<vector<int> > &arr, int n, int m){
	
	vector<int>ans;
	int size = n*m;

	for(int i =0;i<n;i++){
		for(int j =0;j<m;j++){
			ans.push_back(arr[i][j]);
		}
	}
	sort(ans.begin(),ans.end());
	return ans[size/2];
	
}
